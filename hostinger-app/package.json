{
  "name": "pezo-sales-app",
  "version": "1.0.0",
  "private": true,
  "description": "Pezo salepage, checkout and admin",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check lib/vault.js && node --check lib/db.js && node --check lib/tool-engine.js",
    "build": "npm run check"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "cookie-parser": "^1.4.7",
    "docx": "^9.7.1",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "jszip": "^3.10.1",
    "multer": "^2.2.0",
    "pg": "^8.23.0"
  }
}
